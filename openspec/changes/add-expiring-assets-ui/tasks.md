## 1. 临期资产配置与基础组件

- [x] 1.1 新增临期资产筛选状态类型和默认状态工厂，固定区、镇街、到期期限与资产类别默认值
- [x] 1.2 新增区和三十二个镇街的完整配置，保持规格规定的内容与顺序
- [x] 1.3 新增四个到期期限和二十个资产类别的完整配置，保持默认值、顺序、标点与末尾箭头语义
- [x] 1.4 复核并按需扩展 `CompactFilterOptionGroup` 的可复用能力，确保不回归交易动态现有布局与交互

## 2. 路由、导航与页面骨架

- [x] 2.1 新增 `/expiring-assets` 命名路由和 `ExpiringAssetsView`
- [x] 2.2 将共享主导航“临期资产”接入新路由并验证当前激活态不改变普通导航项尺寸
- [x] 2.3 在页面中复用站点头部、主导航并实现“当前位置 首页 / 临期资产”面包屑
- [x] 2.4 在筛选区下方接入统一 `ComingSoon` 结果占位并保留 `BackToTop`
- [x] 2.5 确认页面不渲染“关于我们”或“监督投诉”页脚板块

## 3. 基础筛选面板与状态交互

- [x] 3.1 新增 `ExpiringAssetsFilterPanel`，实现“已选条件：”和右侧“全部撤销”头部
- [x] 3.2 按截图实现区和镇街筛选行的内容、换行、红底白字单选态
- [x] 3.3 按截图实现到期期限筛选行，并将“一年内到期”设为默认选中项
- [x] 3.4 按截图实现资产类别两行布局、完整选项和第一行末尾细灰色箭头
- [x] 3.5 实现四组本地单选及非默认选择的已选条件标签，不裁剪或联动其他选项
- [x] 3.6 实现“全部撤销”，恢复四组规定默认值并清空条件标签
- [x] 3.7 验证所有基础筛选不修改路由、不发起请求且不改变 `ComingSoon`

## 4. 更多选项与关键字交互

- [x] 4.1 实现贴合面板底边的“更多选项(关键字)”按钮及初始向下箭头
- [x] 4.2 实现点击展开和再次点击收起，并同步向上/向下箭头、`aria-expanded` 与关联区域
- [x] 4.3 在展开区实现原站风格关键字输入与“确定”入口
- [x] 4.4 区分关键字输入值和已确认值，确认后在已选条件区域显示关键字条件
- [x] 4.5 确保收起再展开保留关键字状态，全部撤销清空关键字但保持当前展开/收起状态
- [x] 4.6 验证关键字输入、确认和展开收起均不发起查询且结果始终为 `ComingSoon`

## 5. 视觉精调与响应式验证

- [x] 5.1 对照参考截图精调正文宽度、面包屑高度、面板边框、标签列宽、字体、颜色和纵横间距
- [x] 5.2 精调镇街与资产类别的自然换行，确保 1366、1440、1920 像素宽视口文字完整且无横向溢出
- [x] 5.3 精调关键字展开行和动态面板底边，确保更多选项按钮不覆盖控件或结果占位
- [x] 5.4 在三种桌面视口分别完成收起态和展开态视觉比对并修正明显偏差

## 6. 自动化测试与交付验证

- [x] 6.1 新增端到端测试，覆盖首页入口、路由、面包屑、临期资产激活态及跨页面导航尺寸稳定性
- [x] 6.2 新增端到端测试，精确校验四组筛选选项、顺序和默认选中态
- [x] 6.3 新增端到端测试，覆盖基础单选、已选条件和全部撤销
- [x] 6.4 新增端到端测试，覆盖更多选项展开/收起、可访问状态、关键字确认、状态保留与撤销
- [x] 6.5 新增端到端测试，验证结果始终敬请期待、无真实列表/暂无公开数据/页脚及无原站或业务请求
- [x] 6.6 新增多视口布局断言，验证收起与展开状态均无横向溢出且“顶部”按钮可用
- [x] 6.7 运行类型检查、生产构建和全部 Playwright 回归测试并修复失败项
- [x] 6.8 更新 OpenSpec 实施任务状态，确认所有验收场景完成后提交并推送实现
